var gulp = require('gulp');

gulp.task('default', function() {
  // we want nodemon server restart
  // scss to css conversion
  // webpack build
  // watch for html,css,js, jsx file changes
  // clean build directory task
  // minifier/uglifier
})
